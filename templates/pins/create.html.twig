{% extends 'base.html.twig' %}

{% block title 'Find all the best pins on earth' %}

{% block body %}
	<h1>Create Pin</h1>
		{{ form_start(monForm) }}

			{{ form_widget(monForm)}}
			<input type="submit" value="Create Pin">

	{{ form_end(monForm) }}
{% endblock %}
